In jobs_controller:
@jobs = Job.find_by(city: xxx)

In view:
<%# @jobs.each do |job|
job.title
job.description
job.level_of_interest
Company.find(job.company_id).name
Category.find(job.category_id).title
<end>
