<h1>Welcome to Job Tracker</h1>
<p>In this site you'll find job opportunities to work as a software developer nationwide.</p>

<h3>All Companies</h3>

<ul>
  <% @companies.each do |company| %>
    <div class=company_<%= company.id %>>
      <li><%= link_to company.name, company_path(company) %>
      <%= link_to '<button type="button">Edit</button>'.html_safe, edit_company_path(company) %>
      <%= link_to '<button type="button">Delete</button>'.html_safe, company_path(company), method: "delete" %>
    </div>
<% end %>
</ul>

<h3><%= link_to "Go To Dashboard", dashboard_path %></h3>
